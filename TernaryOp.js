let user_is_authenticated = true;

user_is_authenticated ? console.log("show a logout") : console.log("show a LogIn");



switch (XPathExpression) {
    case XPathExpression:
        if(this.state.user_is_authenticated)user_is_authenticated
        break;user_is_authenticated
    default:
        break;
}